#include "common.h"

void kmer_compare(string file, const int peak1, const double thr_num1, const int thr_num2, string output);
void kmer_compare(string file1, string file2, int peak1, int peak2, double thr_num1, int thr_num2, string output);
